<div class="page-layout simple fullwidth" pulsePerfectScrollbar>
    <div class="container mt-12">
        <div class="content-wrapper">
            <div class="page-title-wrapper" fxLayout="row" fxLayoutAlign="space-between center">
                <div class="pulse-main-heading">Payroll Report QB</div>
                <div class="payroll-totals" fxLayout="row">
                    <form (submit)="fetchGridData()" fxLayout="row">
                        <div class="form-container">
                            <dx-form id="form1" #dxPayrollDataFilter [showColonAfterLabel]="true" [colCount]="1"
                                [formData]="payrollDataFilter" labelLocation="left">
                                <dxi-item dataField="status" editorType="dxSelectBox"
                                    [editorOptions]="{ items: payrollFilterValueMain.status, placeholder: 'Select Status', value: 'All'}">
                                    <!--<dxo-label text="Status"></dxo-label>-->
                                </dxi-item>
                            </dx-form>
                        </div>
                        <div class="buttons-container ml-16" fxLayout="row" fxLayoutAlign="end end">
                            <dx-button text="Search" class="pulse-btn pulse-btn-primary" [useSubmitBehavior]="true">
                            </dx-button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="p-30 pt-0 pb-15 grid-wrapper" style="margin-top: 5px;">
                <dx-form id="form" #dxFilterForm labelLocation="right" [formData]="qbFilterForm">
                    <dxi-item itemType="group" [colCount]="7" cssClass="test">
                        <dxi-item dataField="frmInQB" editorType="dxCheckBox" cssClass="in-qb payroll-qb" [colSpan]="2"
                            [editorOptions]="{ onValueChanged: filterConsultantName}">
                            <dxo-label class="" text="In QB"></dxo-label>
                        </dxi-item>
                        <dxi-item dataField="frmNotInQB" editorType="dxCheckBox" cssClass="not-in-qb payroll-qb"
                            [colSpan]="3" [editorOptions]="{onValueChanged: filterConsultantName}">
                            <dxo-label text="Not In QB"></dxo-label>
                        </dxi-item>
                        <dxi-item dataField="frmInDB" editorType="dxCheckBox" cssClass="payroll-qb in-db" [colSpan]="2"
                            [editorOptions]="{onValueChanged: filterConsultantName}">
                            <dxo-label text="In DB"></dxo-label>
                        </dxi-item>
                    </dxi-item>
                </dx-form>
                <h5>Consultant List</h5>
                <dx-list [height]="400" activeStateEnabled="true"
                    [dataSource]="applyFilter ? filterData : consultantNames" itemTemplate="listItem"
                    (onItemClick)="loadData($event)">
                    <div *dxTemplate="let item of 'listItem'">
                        <div fxLayout="row" fxLayoutAlign="space-between">
                            <!-- [class.active]="selectedIndex === i" -->
                            <div>{{item.ItemName}} - {{item.CandidateType}}</div>
                            <span [ngClass]="{'green-dot': item.QBReffID, 'red-dot': !item.QBReffID}"></span>
                            <!-- <dx-check-box value="item.new"></dx-check-box> -->
                        </div>
                    </div>
                </dx-list>
            </div>
        </div>
    </div>
</div>
